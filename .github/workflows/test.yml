name: QA Automation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout kode
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install fastapi uvicorn httpx2 pytest pytest-cov sqlalchemy

      - name: Jalankan test
        env:
          PYTHONPATH: .
        run: |
          pytest tests/ -v